/*!
 * Sitemap
 * Copyright(c) 2011 Eugene Kalinin
 * MIT Licensed
 */
'use strict';
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const padStart_1 = __importDefault(require("lodash/padStart"));
function getTimestampFromDate(dt, bRealtime) {
    let timestamp = [dt.getUTCFullYear(), padStart_1.default((dt.getUTCMonth() + 1), 2, '0'),
        padStart_1.default(dt.getUTCDate(), 2, '0')].join('-');
    // Indicate that lastmod should include minutes and seconds (and timezone)
    if (bRealtime && bRealtime === true) {
        timestamp += 'T';
        timestamp += [padStart_1.default(dt.getUTCHours(), 2, '0'),
            padStart_1.default(dt.getUTCMinutes(), 2, '0'),
            padStart_1.default(dt.getUTCSeconds(), 2, '0')
        ].join(':');
        timestamp += 'Z';
    }
    return timestamp;
}
exports.getTimestampFromDate = getTimestampFromDate;
//# sourceMappingURL=utils.js.map